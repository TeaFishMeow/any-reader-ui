定义12.2.11（线性相关和线性无关）如果存在不全为零的实数 $k_{1}, k_{2}, \dots, k_{m}$ 使

$$
k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {m} \alpha_ {m} = \theta ,
$$

则说向量组 $\alpha_{1},\alpha_{2},\dots \alpha_{m}$ 线性相关.如果只有当 $k_{1} = k_{2} = \dots = k_{m} = 0$ 时上述等式才成立，则说这组向量线性无关(或线性独立).

由定义可见，一组分量个数相同的向量不是线性相关就是线性无关。而且线性相关与否既跟这些向量的次序无关，也跟这些向量是一概写为行向量还是一概写为列向量没有关系。关于线性相关和线性无关，下列结论是最基本的。

1) 一个向量 $\alpha$ 线性相关的充要条件是 $\alpha = \theta$ .

证 若 $\alpha = \theta$ ，可任取 $k \neq 0$ ，使 $k\alpha = \theta$ ；反之，若 $k \neq 0, \alpha \neq \theta$ 则 $k\alpha \neq \theta$ ，故要存在 $k \neq 0$ ，使 $k\alpha = \theta$ ，则必 $\alpha = \theta$ □

所证明的事实也可说成：一个向量 $\alpha$ 线性无关的充要条件是 $\alpha \neq \theta$

2) 包含零向量的向量组必定线性相关.

证 不失一般性，考虑向量组 $\alpha_{1},\alpha_{2},\dots ,\alpha_{m},\theta$ ，取 $k_{1} = k_{2} = \dots = k_{m} = 0,k_{m + 1} = 1$ 则

$$
k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {m} \alpha_ {m} + k _ {m + 1} \theta = \theta ,
$$

即 $\alpha_{1},\alpha_{2},\dots ,\alpha_{m},\theta$ 线性相关

3) 若向量组 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{l}$ 线性相关，则随意增添几个分量个数与诸 $\alpha_{i}$ 相同的向量 $\alpha_{l+1}, \dots, \alpha_{m}$ 后所得的向量组 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{l}, \alpha_{l+1}, \dots, \alpha_{m}$ 仍然线性相关.

证 由 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{l}$ 线性相关，存在不全零的实数 $k_{1}, k_{2}, \dots, k_{l}$ 使

$$
k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {l} \alpha_ {l} = \theta ,
$$

取 $k_{l + 1} = \dots = k_m = 0$ ，则

$$
k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {l} \alpha_ {l} + k _ {l + 1} \alpha_ {l + 1} + \dots + k _ {m} \alpha_ {m} = \theta ,
$$

且 $k_{1}, k_{2}, \dots, k_{l}, k_{l+1}, \dots, k_{m}$ 不全为零，故 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{l}, \alpha_{l+1}, \dots, \alpha_{m}$ 线性相关. □

所证明的结果显然可以说成

4) 若向量组 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{m}$ 线性无关，则其中一部分向量所成的向量组也线性无关.

例12.2.14 证明向量组 $\alpha_{1} = (8,3, - 3,4)$ ， $\alpha_{2} = (4,0, - 1,1)$ ， $\alpha_{3} = (0,3,1,2)$ 线性相关.

证 取 $k_{1} = -1, k_{2} = 2, k_{3} = 1$ 则

$$
\begin{array}{l} k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + k _ {3} \alpha_ {3} \\ = - (8, 3, - 3, 4) + 2 (4, 0, - 1, 1) + (0, 3, - 1, 2) = (0, 0, 0, 0), \\ \end{array}
$$

故 $\alpha_{1},\alpha_{2},\alpha_{3}$ 线性相关

例12.2.15 试证向量组

$$
e _ {1} = \left( \begin{array}{c} 1 \\ 0 \\ 0 \\ 0 \end{array} \right), \quad e _ {2} = \left( \begin{array}{c} 0 \\ 1 \\ 0 \\ 0 \end{array} \right), \quad e _ {3} = \left( \begin{array}{c} 0 \\ 0 \\ 1 \\ 0 \end{array} \right), \quad e _ {4} = \left( \begin{array}{c} 0 \\ 0 \\ 0 \\ 1 \end{array} \right)
$$

线性无关.

证因为

$$
\begin{array}{l} k _ {1} e _ {1} + k _ {2} e _ {2} + k _ {3} e _ {3} + k _ {4} e _ {4} \\ = k _ {1} \left( \begin{array}{l} 1 \\ 0 \\ 0 \\ 0 \end{array} \right) + k _ {2} \left( \begin{array}{l} 0 \\ 1 \\ 0 \\ 0 \end{array} \right) + k _ {3} \left( \begin{array}{l} 0 \\ 0 \\ 1 \\ 0 \end{array} \right) + k _ {4} \left( \begin{array}{l} 0 \\ 0 \\ 0 \\ 1 \end{array} \right) = \left( \begin{array}{l} k _ {1} \\ k _ {2} \\ k _ {3} \\ k _ {4} \end{array} \right), \\ \end{array}
$$

故 $k_{1}e_{1} + k_{2}e_{2} + k_{3}e_{3} + k_{4}e_{4} = \theta$ 即 $(k_{1},k_{2},k_{3},k_{4})^{\mathrm{T}} = (0,0,0,0)^{\mathrm{T}}$ ，这仅当 $k_{1} = k_{2} = k_{3} = k_{4} = 0$ 时才成立，这证明 $e_1,e_2,e_3,e_4$ 线性无关. □

称第 $i$ 个 $(i = 1,2,\dots ,n)$ 分量为1其余分量全为零的具 $n$ 个分量的向量为 $n$ 维基本向量，记为 $e_i$ ，即 $e_1 = (1,0,0,\dots ,0)^{\mathrm{T}},e_2 = (0,1,0,\dots ,0)^{\mathrm{T}},\dots ,e_n =$ $(0,0,0,\dots ,1)^{\mathrm{T}}$ .仿照例12.2.15可以证明， $n$ 个 $n$ 维基本向量 $e_1,e_2,\dots ,e_n$ 是线性无关的，称之为基本向量组

例12.2.16 设向量组 $\alpha_{1},\alpha_{2},\alpha_{3}$ 线性无关，而

$$
\beta_ {1} = \alpha_ {1} + \alpha_ {2}, \quad \beta_ {2} = \alpha_ {2} + \alpha_ {3}, \quad \beta_ {3} = \alpha_ {3} + \alpha_ {1},
$$

证明向量组 $\beta_{1},\beta_{2},\beta_{3}$ 也线性无关

证 若常数 $k_{1}, k_{2}, k_{3}$ 使

$$
k _ {1} \beta_ {1} + k _ {2} \beta_ {2} + k _ {3} \beta_ {3} = \theta ,
$$

即

$$
k _ {1} (\alpha_ {1} + \alpha_ {2}) + k _ {2} (\alpha_ {2} + \alpha_ {3}) + k _ {3} (\alpha_ {3} + \alpha_ {1}) = \theta ,
$$

将此式重新整理得

$$
(k _ {1} + k _ {3}) \alpha_ {1} + (k _ {1} + k _ {2}) \alpha_ {2} + (k _ {2} + k _ {3}) \alpha_ {3} = \theta .
$$

但按假设， $\alpha_{1},\alpha_{2},\alpha_{3}$ 线性无关，欲上式成立，必须

$$
\left\{ \begin{array}{l} k _ {1} + k _ {3} = 0, \\ k _ {1} + k _ {2} = 0, \\ k _ {2} + k _ {3} = 0. \end{array} \right. \tag {12.4}
$$

将方程组（12.4）的三个方程相加可知

$$
k _ {1} + k _ {2} + k _ {3} = 0,
$$

此式分别与（12.4）的三个方程相减，依次得到

$$
k _ {2} = 0, \quad k _ {3} = 0, \quad k _ {1} = 0,
$$

这表示 $\beta_{1},\beta_{2},\beta_{3}$ 是线性无关的.

在12.3节，将用更简单更一般的方法说明方程组（12.4）的解为 $k_{1} = k_{2} = k_{3} = 0$

直接由定义出发，判定向量组的线性相关还是线性无关，当向量的个数较多时，一般说来是困难的。下面的定理使我们得以通过矩阵的初等变换以判定向量组

线性相关与否. 在读了 12.3 节以后, 读者可自行给出这定理的证明 (习题 12.3 第 7 题 1)).

定理12.2.3 向量组

$$
\alpha_ {1} = \left( \begin{array}{c} a _ {1 1} \\ a _ {2 1} \\ \vdots \\ a _ {n 1} \end{array} \right), \quad \alpha_ {2} = \left( \begin{array}{c} a _ {1 2} \\ a _ {2 2} \\ \vdots \\ a _ {n 2} \end{array} \right), \quad \dots , \quad \alpha_ {m} = \left( \begin{array}{c} a _ {1 m} \\ a _ {2 m} \\ \vdots \\ a _ {n m} \end{array} \right),
$$

线性无关的充要条件是矩阵

$$
\boldsymbol {A} = \left( \begin{array}{c c c c} a _ {1 1} & a _ {1 2} & \dots & a _ {1 m} \\ a _ {2 1} & a _ {2 2} & \dots & a _ {2 m} \\ \vdots & \vdots & & \vdots \\ a _ {n 1} & a _ {n 2} & \dots & a _ {n m} \end{array} \right)
$$

的秩 $r$ 等于向量的个数 $m$ .

由定理12.2.3可知，为了判断 $m$ 个列向量 $\alpha_{1},\alpha_{2},\dots ,\alpha_{m}$ 是否线性相关，只需求出以这些向量为列向量组的矩阵的秩 $r,$ 若 $r = m,$ 则线性无关，若 $r <   m,$ 则线性相关.

例12.2.17 判断向量组

$$
\alpha_ {1} = \left( \begin{array}{c} - 1 \\ 5 \\ 3 \\ - 2 \\ 1 \end{array} \right), \quad \alpha_ {2} = \left( \begin{array}{c} 4 \\ 1 \\ - 2 \\ 9 \\ 7 \end{array} \right), \quad \alpha_ {3} = \left( \begin{array}{c} 0 \\ 3 \\ 4 \\ - 5 \\ - 1 \end{array} \right), \quad \alpha_ {4} = \left( \begin{array}{c} 2 \\ 0 \\ - 1 \\ 4 \\ 3 \end{array} \right)
$$

是否线性相关.

解将以 $\alpha_{1},\alpha_{2},\alpha_{3},\alpha_{4}$ 为列向量组的矩阵用初等行变换化为阶梯形矩阵：

$$
\begin{array}{l} \left( \begin{array}{c c c c} - 1 & 4 & 0 & 2 \\ 5 & 1 & 3 & 0 \\ 3 & - 2 & 4 & - 1 \\ - 2 & 9 & - 5 & 4 \\ 1 & 7 & - 1 & 3 \end{array} \right) \xrightarrow [ \longleftarrow ]{R _ {2} + 5 R _ {1}} \left( \begin{array}{c c c c} - 1 & 4 & 0 & 2 \\ 0 & 21 & 3 & 10 \\ 0 & 10 & 4 & 5 \\ 0 & 1 & - 5 & 0 \\ 0 & 11 & - 1 & 5 \end{array} \right) \\ \begin{array}{r} \xrightarrow {R _ {2} \longleftrightarrow R _ {4}} \left( \begin{array}{c c c c} - 1 & 4 & 0 & 2 \\ 0 & 1 & - 5 & 0 \\ 0 & 10 & 4 & 5 \\ 0 & 21 & 3 & 10 \\ 0 & 11 & - 1 & 5 \end{array} \right) \xrightarrow [ \xleftarrow {R _ {3} - 10 R _ {2}} ]{R _ {3} - 10 R _ {2}} \left( \begin{array}{c c c c} - 1 & 4 & 0 & 2 \\ 0 & 1 & - 5 & 0 \\ 0 & 0 & 54 & 5 \\ 0 & 0 & 108 & 10 \\ 0 & 0 & 54 & 5 \end{array} \right) \end{array} \\ \xleftarrow {R _ {4} - 2 R _ {3}} \left( \begin{array}{c c c c} - 1 & 4 & 0 & 2 \\ 0 & 1 & - 5 & 0 \\ 0 & 0 & 54 & 5 \\ 0 & 0 & 0 & 0 \\ 0 & 0 & 0 & 0 \end{array} \right). \\ \end{array}
$$

最后的阶梯形矩阵有三个非零行，故秩为3,小于向量的个数4，因而 $\alpha_{1},\alpha_{2},\alpha_{3},\alpha_{4}$ 线性相关.

由定理12.2.3可得如下推论：

推论1 若向量的个数 $m$ 大于维数 $n$ , 则向量组线性相关

证 因为 $\operatorname{rank} A \leqslant \min(m, n) < m$ , 故推论 1 成立.

由推论1可知，任何 $n + 1$ 个 $n$ 维向量都是线性相关的

因为矩阵为满秩矩阵的充要条件是其行列式不为零，于是又得

推论2 $n$ 个 $n$ 维向量 $\alpha_{1},\alpha_{2},\dots ,\alpha_{n}$ 线性无关(相关)的充要条件是行列式 $|\alpha_{1}\alpha_{2}\dots \alpha_{n}|$ 不为 (为)零.

推论3 若 $A = (a_{ij})_{n \times m}$ 的秩 $r < m$ ，且由 $A$ 的某 $r$ 列所组成的矩阵含有不等于零的 $r$ 阶子式，则此 $r$ 个列向量线性无关.

证不妨设 $\pmb{A}$ 的前 $r$ 列 $\alpha_{1},\alpha_{2},\dots ,\alpha_{r}$ 所成的矩阵含有非零的 $r$ 阶子式，则

$$
\left( \begin{array}{c c c c} a _ {1 1} & a _ {1 2} & \dots & a _ {1 r} \\ a _ {2 1} & a _ {2 2} & \dots & a _ {2 r} \\ \vdots & \vdots & & \vdots \\ a _ {n 1} & a _ {n 2} & \dots & a _ {n r} \end{array} \right)
$$

的秩为 $r$ , 等于向量组 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{r}$ 所含向量的个数, 由定理 12.2.3, 这 $r$ 个向量线性无关.

定义12.2.12 如果存在实数 $k_{1}, k_{2}, \dots, k_{s}$ , 使得

$$
\beta = k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {s} \alpha_ {s},
$$

则说向量 $\beta$ 是向量 $\alpha_{1},\alpha_{2},\dots ,\alpha_{s}$ 的线性组合，或说 $\beta$ 可由 $\alpha_{1},\alpha_{2},\dots ,\alpha_{s}$ 线性表出.

例12.2.18 设

$$
\alpha_ {1} = \left( \begin{array}{c} 1 \\ 0 \\ - 2 \\ 3 \end{array} \right), \quad \alpha_ {2} = \left( \begin{array}{c} - 1 \\ 2 \\ 1 \\ 4 \end{array} \right), \quad \alpha_ {3} = \left( \begin{array}{c} 1 \\ 2 \\ - 3 \\ 10 \end{array} \right).
$$

则由于 $\alpha_{3} = 2\alpha_{1} + \alpha_{2}$ ，因而 $\alpha_{3}$ 是 $\alpha_{1},\alpha_{2}$ 的线性组合.同样，由于 $\alpha_{2} = \alpha_{3} - 2\alpha_{1}$ $\alpha_{1} = \frac{1}{2} (\alpha_{3} - \alpha_{2})$ ，可知 $\alpha_{2}$ 是 $\alpha_{1},\alpha_{3}$ 的线性组合， $\alpha_{1}$ 也是 $\alpha_{2},\alpha_{3}$ 的线性组合.

由定义12.2.12可知：

1) $n$ 维零向量可经任何 $n$ 维向量组线性表出：

$$
\boldsymbol {\theta} = 0 \cdot \alpha_ {1} + 0 \cdot \alpha_ {2} + \dots + 0 \cdot \alpha_ {s}.
$$

2) 任何 $n$ 维向量 $\alpha = (a_{1}, a_{2}, \dots, a_{n})^{\mathrm{T}}$ 都可经基本向量组 $e_{1}, e_{2}, \dots, e_{n}$ 线性表出：

$$
\alpha = a _ {1} e _ {1} + a _ {2} e _ {2} + \dots + a _ {n} e _ {n}.
$$

实际上成立更一般的结论：若 $\alpha_{1},\alpha_{2},\dots ,\alpha_{n}$ 是线性无关的 $n$ 维向量，则任何 $n$ 维向量都可由向量组 $\alpha_{1},\alpha_{2},\dots ,\alpha_{n}$ 线性表出，并且表示式是唯一的（见习题12.3第7题2))

定理12.2.4 向量组 $\alpha_{1},\alpha_{2},\dots ,\alpha_{s}$ 线性相关的充要条件是其中至少有一个向量可由其余向量线性表出.

证 若 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{s}$ 线性相关，则存在不全为零的实数 $k_{1}, k_{2}, \dots, k_{s}$ 使

$$
k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {s} \alpha_ {s} = \theta .
$$

若 $k_{i}\neq 0$ 则

$$
\alpha_ {i} = - \frac {k _ {1}}{k _ {i}} \alpha_ {1} - \dots - \frac {k _ {i - 1}}{k _ {i}} \alpha_ {i - 1} - \frac {k _ {i + 1}}{k _ {i}} \alpha_ {i + 1} - \dots - \frac {k _ {s}}{k _ {i}} \alpha_ {s},
$$

于是 $\alpha_{i}$ 可由 $\alpha_{1},\dots ,\alpha_{i - 1},\alpha_{i + 1},\dots ,\alpha_{s}$ 线性表出.

反之，若某个向量 $\alpha_{i}$ 可由其余向量线性表出，即

$$
\alpha_ {i} = k _ {1} \alpha_ {1} + \dots + k _ {i - 1} \alpha_ {i - 1} + k _ {i + 1} \alpha_ {i + 1} + \dots + k _ {s} \alpha_ {s},
$$

则

$$
k _ {1} \alpha_ {1} + \dots + k _ {i - 1} \alpha_ {i - 1} - \alpha_ {i} + k _ {i + 1} \alpha_ {i + 1} + \dots + k _ {s} \alpha_ {s} = \theta
$$

且 $k_{1}, k_{2}, \dots, k_{i-1}, -1, k_{i+1}, \dots, k_{s}$ 不全为零，故 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{s}$ 线性相关.

例12.2.19 设 $\alpha_{1},\alpha_{2},\dots ,\alpha_{m - 1}$ （ $m\geqslant 3$ )线性相关，而 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m}$ 线性无关，试证：

(1) $\alpha_{m}$ 可由 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m - 1}$ 线性表出；  
(2) $\alpha_{m}$ 不能由 $\alpha_{1},\alpha_{2},\dots ,\alpha_{m - 1}$ 线性表出.

证（1）因 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m}$ 线性无关，故其部分组 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m - 1}$ 线性无关.又因 $\alpha_{1},\alpha_{2},\dots ,\alpha_{m - 1}$ 线性相关，所以存在不全为零的数 $k_{1},k_{2},\dots ,k_{m - 1}$ 使得

$$
k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {m - 1} \alpha_ {m - 1} = \theta ,
$$

其中 $k_{1}$ 不会为零（因若 $k_{1} = 0$ ，则 $k_{2}, k_{3}, \dots, k_{m-1}$ 不全为零，使 $k_{2} \alpha_{2} + k_{3} \alpha_{3} + \dots + k_{m-1} \alpha_{m-1} = \theta,$ 从而 $\alpha_{2}, \alpha_{3}, \dots, \alpha_{m-1}$ 线性相关，与已知条件矛盾）于是，由此得

$$
\alpha_ {1} = - \frac {k _ {2}}{k _ {1}} \alpha_ {2} - \frac {k _ {3}}{k _ {1}} \alpha_ {3} - \dots - \frac {k _ {m - 1}}{k _ {1}} \alpha_ {m - 1},
$$

即 $\alpha_{1}$ 可由 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m - 1}$ 线性表出.

(2) 设 $\alpha_{m}$ 可由 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{m-1}$ 线性表出，令

$$
\alpha_ {m} = l _ {1} \alpha_ {1} + l _ {2} \alpha_ {2} + \dots + l _ {m - 1} \alpha_ {m - 1},
$$

又由(1)所证， $\alpha_{1}$ 可由 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m - 1}$ 线性表出，令

$$
\alpha_ {1} = c _ {2} \alpha_ {2} + c _ {3} \alpha_ {3} + \dots + c _ {m - 1} \alpha_ {m - 1},
$$

将此式代入上式，得

$$
\alpha_ {m} = \left(l _ {1} c _ {2} + l _ {2}\right) \alpha_ {2} + \left(l _ {1} c _ {3} + l _ {3}\right) \alpha_ {3} + \dots + \left(l _ {1} c _ {m - 1} + l _ {m - 1}\right) \alpha_ {m - 1},
$$

即 $\alpha_{m}$ 是 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m - 1}$ 的线性组合，因而 $\alpha_{2},\alpha_{3},\dots ,\alpha_{m}$ 线性相关，这与已知条件矛盾．这表明 $\alpha_{m}$ 不能由 $\alpha_{1},\alpha_{2},\dots ,\alpha_{m - 1}$ 线性表出.

定理12.2.5 若 $\alpha_{1},\alpha_{2},\dots ,\alpha_{s}$ 线性无关，而 $\alpha_{1},\alpha_{2},\dots ,\alpha_{s},\alpha$ 线性相关，则 $\pmb{\alpha}$ 可以由 $\alpha_{1},\alpha_{2},\dots ,\alpha_{s}$ 线性表出，并且表示方式是唯一的.

证 由 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{s}, \alpha$ 线性相关，知存在不全为零的数 $k_{1}, k_{2}, \dots, k_{s}, k$ 使得

$$
k _ {1} \alpha_ {1} + k _ {2} \alpha_ {2} + \dots + k _ {s} \alpha_ {s} + k \alpha = \theta .
$$

于此，若 $k = 0$ ，则 $k_{1}, k_{2}, \dots, k_{s}$ 不全为零，因而上式表示 $\alpha_{1}, \alpha_{2}, \dots, \alpha_{s}$ 线性相关．这与已知矛盾，所以 $k \neq 0$ 从而得

$$
\alpha = - \frac {k _ {1}}{k} \alpha_ {1} - \frac {k _ {2}}{k} \alpha_ {2} - \dots - \frac {k _ {s}}{k} \alpha_ {s},
$$

即 $\alpha$ 可由 $\alpha_{1},\alpha_{2},\dots ,\alpha_{s}$ 线性表出

若表示式不是唯一的，设同时成立

$$
\begin{array}{l} \alpha = c _ {1} \alpha_ {1} + c _ {2} \alpha_ {2} + \dots + c _ {s} \alpha_ {s}, \\ \alpha = l _ {1} \alpha_ {1} + l _ {2} \alpha_ {2} + \dots + l _ {s} \alpha_ {s}, \\ \end{array}
$$

并且存在 $i(1 \leqslant i \leqslant s)$ 使得 $c_{i} \neq l_{i}$ , 将上两式相减得

$$
\left(c _ {1} - l _ {1}\right) \alpha_ {1} + \left(c _ {2} - l _ {2}\right) \alpha_ {2} + \dots + \left(c _ {s} - l _ {s}\right) \alpha_ {s} = \theta ,
$$

$c_{j} - l_{j}$ $(j = 1,2,\dots ,s)$ 不全为零，于是， $\alpha_{1},\alpha_{2},\dots ,\alpha_{s}$ 线性相关.这与假设矛盾这就证明了表示式是唯一的.
