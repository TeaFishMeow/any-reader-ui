# any-reader-ui

Standalone UI draft for the AnyReader reading interface.

The local reader loads Markdown and attachments from `微积分二层次下` through the Vite dev server. No mock API server is required.

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:1420/`.
