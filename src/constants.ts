export const LEFT_DEFAULT = 280
export const RAIL_WIDTH = 36
export const READER_WIDTH = 780
export const PERSIST_DELAY_MS = 450
export const VIEWPORT_HEIGHT = () => (typeof window === 'undefined' ? 720 : window.innerHeight)
